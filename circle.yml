machine:
  services:
    - docker

dependencies:
  pre:
    - sudo pip install awscli

test:
  override:
    - hack/jenkins/build.sh:
        timeout: 900
    - hack/jenkins/e2e-simple.sh:
        timeout: 900
        environment:
          KUBE_SKIP_CONFIRMATIONS: y
          KUBERNETES_PROVIDER: aws
          KUBE_SKIP_PUSH_DOCKER: y
          KUBE_SKIP_PUSH_GCS: y
          E2E_CLUSTER_NAME: jenkins
          E2E_NETWORK: ""
          E2E_ZONE: us-west-2
          E2E_OPT: ""
