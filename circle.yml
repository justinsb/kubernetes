machine:
  services:
    - docker

dependencies:
  pre:
    - sudo pip install awscli

test:
  override:
    - hack/e2e-test.sh:
        timeout: 900
        environment:
          KUBE_SKIP_CONFIRMATIONS: y
          KUBERNETES_PROVIDER: aws
