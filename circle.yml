machine:
  services:
    - docker

dependencies:
  pre:
    - sudo pip install awscli

test:
  override:
    - hack/jenkins/e2e-simple.sh
        timeout: 900
        environment:
          KUBE_SKIP_CONFIRMATIONS: y
          KUBERNETES_PROVIDER: aws
